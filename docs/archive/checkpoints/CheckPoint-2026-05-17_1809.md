# Checkpoint: 2026-05-17_1809

## Context Summary

Session focused on getting the dev environment fully operational after a cold start.
No feature work was done; this was pure environment setup and test triage.

## Accomplishments

- Fixed `requirements.txt` encoding (UTF-16 LE -> UTF-8); pip could not parse it before.
- Installed all 38 runtime + dev packages into the venv (Python 3.13.12).
- Confirmed all quality-gate tools present: pytest, ruff, black, mypy, pre-commit.
- Fixed pre-existing test failure: `test_publish_file_moves_file_after_post`.
- Test suite now 12/12 passing.

## Technical Changes

### requirements.txt
- Re-encoded from UTF-16 LE to UTF-8. Content unchanged.

### tests/test_post_pusher.py (line 148)
- `test_publish_file_moves_file_after_post`: added `pre-post/` subdirectory to
  tmp_path so the directory structure matches the real `content/pre-post/` ->
  `content/posted/` path that `post_pusher.py:120` expects.

```diff
-    html_file = tmp_path / "moved-post.html"
+    pre_post_dir = tmp_path / "pre-post"
+    pre_post_dir.mkdir()
+    html_file = pre_post_dir / "moved-post.html"
```

## Known Issues

- `pre-commit install` has not been run yet this session (hooks not active in venv).
  Run `venv\Scripts\pre-commit install --hook-type pre-commit --hook-type pre-push`
  before next commit on a new machine / fresh venv.

## Git Status

- `requirements.txt` modified (encoding fix, unstaged).
- `tests/test_post_pusher.py` modified (test fix, unstaged).
- Several untracked files: `.claude/`, `docs/`, `pyproject.toml`,
  `requirements-dev.txt`, `tests/`, `.pre-commit-config.yaml`, `CLAUDE.md`.
- Branch: main, up to date with origin/main.

## Next Session Priorities

1. Run `pre-commit install --hook-type pre-commit --hook-type pre-push`.
2. Pick up next feature or bug fix from backlog.
3. Consider adding tests for `push_it_ui_mvp.py` GUI logic isolation.

## Backlog

- Move credentials from config.json to .env / environment variables.
- Add tests for push_it_ui_mvp.py.
- Add integration test against a local WP test instance.
- CI/CD pipeline (GitHub Actions).
